---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: web.hewlettpackard.com
  kind: Squest
  role: squest
  #watchDependentResources: False
  # Ignore jobs from watcher
  blacklist:
    - group: "batch"
      version: v1
      kind: Job

#+kubebuilder:scaffold:watch
